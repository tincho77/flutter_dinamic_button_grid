import 'package:flutter/material.dart';

class EmptyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dinamic Grid Buttons'),
      ),
    );
  }
}

// this empty page use for adding new page on the App
// easy the process of create a new page
// this is one initial structure  example
